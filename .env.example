# =============================================================================
# Staff Finder Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the required values.
# All variables have sensible defaults except API keys which are required.
# =============================================================================

# -----------------------------------------------------------------------------
# IO Settings
# -----------------------------------------------------------------------------
# INPUT_CSV=schools.csv                    # Input CSV file path
# OUTPUT_CSV=schools_with_staff_links.csv  # Output CSV file path
# SYSTEM_PROMPT_PATH=system_prompt.md      # Path to custom system prompt file

# -----------------------------------------------------------------------------
# OpenAI Settings (Required: OPENAI_API_KEY)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=                            # Required: Your OpenAI API key
# OPENAI_MODEL=gpt-5-mini                  # Model to use for reasoning
# OPENAI_REQUEST_TIMEOUT=30.0              # Request timeout in seconds
# OPENAI_VERBOSITY=low                     # Verbosity level (low, medium, high)
# OPENAI_REASONING_EFFORT=low              # Reasoning effort (low, medium, high)

# -----------------------------------------------------------------------------
# Jina Search Settings (Required: JINA_API_KEY)
# -----------------------------------------------------------------------------
JINA_API_KEY=                              # Required: Your Jina Search API key
# JINA_BASE_URL=https://s.jina.ai          # Jina Search API base URL
# JINA_NO_CACHE=false                      # Disable Jina server-side caching
# JINA_REQUEST_TIMEOUT=30.0                # Request timeout in seconds

# -----------------------------------------------------------------------------
# Search Strategy
# -----------------------------------------------------------------------------
# MAX_QUERIES_PER_SCHOOL=3                 # Maximum search queries per school
# CANDIDATES_FOR_SELECTION=10              # Number of URL candidates to evaluate

# -----------------------------------------------------------------------------
# Concurrency (Three-Tier Rate Limiting)
# -----------------------------------------------------------------------------
# MAX_CONCURRENT_SCHOOLS=5                 # Concurrent school processing
# MAX_CONCURRENT_JINA=10                   # Concurrent Jina API calls
# MAX_CONCURRENT_OPENAI=4                  # Concurrent OpenAI API calls

# -----------------------------------------------------------------------------
# Checkpointing & Resume
# -----------------------------------------------------------------------------
# CHECKPOINT_EVERY=250                     # Save progress every N rows
# ENABLE_RESUME=true                       # Resume from existing output file

# -----------------------------------------------------------------------------
# Local Caching
# -----------------------------------------------------------------------------
# ENABLE_JINA_CACHE=true                   # Enable local Jina response cache
# CACHE_DIR=.cache_jina                    # Cache directory path

# -----------------------------------------------------------------------------
# Retry Settings
# -----------------------------------------------------------------------------
# MAX_RETRIES=3                            # Maximum retry attempts on failure
# RETRY_INITIAL_WAIT=1.0                   # Initial backoff wait in seconds
# RETRY_MAX_WAIT=8.0                       # Maximum backoff wait in seconds

# -----------------------------------------------------------------------------
# Pacing
# -----------------------------------------------------------------------------
# PER_ROW_DELAY_SEC=0.0                    # Delay between processing rows (seconds)

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# LOG_LEVEL=INFO                           # Logging level (DEBUG, INFO, WARNING, ERROR)
